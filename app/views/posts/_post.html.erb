<div id="<%= dom_id post %>">
  <h2><%= post.title %></h2>
  <h5>Posted By <%= post.user.name %></h5>
  <h6><%= pluralize(post.views, "view") %></h6>

  <div>
    <strong>Body:</strong>
    <%= post.body %>
  </div>

</div>
